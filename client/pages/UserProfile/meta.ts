import type { MetaFunction } from "react-router";

const meta: MetaFunction = () => {
  return [
    {
      content: "Lazy Days Spa - User Profile Page",
      name: "description",
      title: "Lazy Days Spa - User Profile",
    },
  ];
};

export { meta };
