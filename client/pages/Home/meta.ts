import type { MetaFunction } from "react-router";

const meta: MetaFunction = () => {
  return [
    {
      content: "Lazy Days Spa - Home Page",
      name: "description",
      title: "Lazy Days Spa",
    },
  ];
};

export { meta };
