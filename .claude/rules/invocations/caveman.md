# Invoke `caveman`

Invoke the `caveman` skill on **every** prompt.

The skill activates ultra-compressed communication mode (drops articles, filler, pleasantries; keeps full technical accuracy; cuts token usage ~75%).

Persistence: once triggered, stays active for the whole session. Switch off only when the user says `stop caveman` or `normal mode`.
